/**
 * Enum representing the different types of pages within the app.
 *
 * - `Note`: Represents a standalone textual note.
 * - `Collection`: Represents a collection of items grouped together based on the same template.
 */
export enum PageType {
  Note = "note",
  Collection = "collection",
}
