/**
 * Represents a Collection Category data structure
 */
export type CollectionCategoryDTO = {
  collectionCategoryID?: number; // represents the collection category ID
  category_name: string; // represents the label of the collection category
  collectionID?: number; // represents the collection ID
};
